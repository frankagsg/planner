# Personal photos

Drop `.jpg`, `.jpeg`, `.png`, `.webp`, or `.gif` files here. They appear in the
personal "Us" photo rotator and (optionally) the screensaver. Files here are
git-ignored by default so private photos aren't committed.
